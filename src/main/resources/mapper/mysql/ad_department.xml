<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ad_department">
    <select id="selectDepartment" resultType="com.devkbil.mtssbj.common.tree.TreeVO" >
        SELECT DEPTNO as KEY, DEPTNM as TITLE, PARENTNO as PARENT
          FROM COM_DEPT
         WHERE DELETEFLAG='N' 
         ORDER BY DEPTNO  
    </select> 
    
    <insert id="insertDepartment" parameterType="com.devkbil.mtssbj.admin.organ.DepartmentVO" >
        INSERT INTO COM_DEPT(DEPTNM, PARENTNO, DELETEFLAG)
        VALUES (#{deptnm}, #{parentno}, 'N')
        <selectKey keyProperty="deptno" resultType="String">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>
    
    <update id="updateDepartment" parameterType="com.devkbil.mtssbj.admin.organ.DepartmentVO">
        UPDATE COM_DEPT
           SET DEPTNM=#{deptnm}
         WHERE DEPTNO=#{deptno}
    </update> 
        
    <select id="selectDepartmentOne" parameterType="String" resultType="com.devkbil.mtssbj.admin.organ.DepartmentVO">
        SELECT DEPTNO, DEPTNM
          FROM COM_DEPT
         WHERE DELETEFLAG='N' AND DEPTNO=#{deptno} 
    </select> 

    <delete id="deleteDepartment" parameterType="String">
        UPDATE COM_DEPT
           SET DELETEFLAG='Y'
         WHERE DEPTNO=#{deptno}
    </delete> 

</mapper>

