<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ad_boardGroup">
    <select id="selectBoardGroupList" resultType="com.devkbil.mtssbj.common.tree.TreeVO">
        SELECT BGNO KEY, BGNAME TITLE, BGPARENT PARENT
        FROM TBL_BOARDGROUP
        WHERE BGDELETEFLAG='N'
        ORDER BY BGNO
    </select>

    <insert id="insertBoardGroup" parameterType="com.devkbil.mtssbj.admin.board.BoardGroupVO">
        INSERT INTO TBL_BOARDGROUP(BGNO, BGNAME, BGUSED, BGREPLY, BGREADONLY, BGNOTICE, BGPARENT, BGDELETEFLAG, BGREGDATE)
        VALUES (BGNO_SEQ.NEXTVAL, #{bgname}, #{bgused}, #{bgreply}, #{bgreadonly}, #{bgnotice}, #{bgparent, jdbcType=VARCHAR}, 'N', SYSDATE)
    </insert>

    <update id="updateBoardGroup" parameterType="com.devkbil.mtssbj.admin.board.BoardGroupVO">
        UPDATE TBL_BOARDGROUP
        SET BGNAME=#{bgname}
          , BGUSED=#{bgused}
          , BGREPLY=#{bgreply}
          , BGREADONLY=#{bgreadonly}
          , BGNOTICE=#{bgnotice}
          , BGUPDATE=SYSDATE
        WHERE BGNO = #{bgno}
    </update>

    <select id="selectBoardGroupOne" parameterType="String" resultType="com.devkbil.mtssbj.admin.board.BoardGroupVO">
        SELECT BGNO, BGNAME, BGUSED, BGREPLY, BGREADONLY, BGNOTICE
        FROM TBL_BOARDGROUP
        WHERE BGDELETEFLAG = 'N'
          AND BGNO = #{bgno}
    </select>

    <delete id="deleteBoardGroup" parameterType="String">
        UPDATE TBL_BOARDGROUP
        SET BGDELETEFLAG='Y',
            BGUPDATE=SYSDATE
        WHERE BGNO = #{bgno}
    </delete>

</mapper>

